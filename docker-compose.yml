name: student-management-backend

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - path: .env.example
      - path: .env
        required: false
    ports:
      - "${BACKEND_HOST_PORT:-8920}:8000"
